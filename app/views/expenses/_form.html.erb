<%= form_for(expense) do |f| %>
  <% if expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expense.errors.count, "error") %> prohibited this expense from being saved:</h2>

      <ul>
      <% expense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= f.label :description, class: 'col-2 col-form-label' %>
    <div class="col-10"><%= f.text_field :description, placeholder: 'groceries', class: 'form-control', required: true %></div>
  </div>

  <div class="form-group row">
    <%= f.label :price, class: 'col-2 col-form-label' %>
    <div class="input-group col-10">
      <div class="input-group-addon">€</div>
      <%= f.number_field :price, placeholder: '10.00', :step => 0.01, class: 'form-control', required: true %>
    </div>
  </div>

  <div class="form-group row">
    <%= f.label :person, class: 'col-2 col-form-label' %>
    <div class="col-10"><%= f.collection_select(:person_id, Person.all, :id, :name, {}, {class: 'form-control', required: true}) %></div>
  </div>

  <div class="form-group row">
    <div class="col-10 offset-2"><%= f.submit nil, class: 'btn btn-primary' %></div>
  </div>
<% end %>
